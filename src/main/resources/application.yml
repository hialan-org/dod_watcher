micronaut:
  application:
    name: dod
    max: 50
  server:
    cors:
      enabled: true
datasources:
    default:
#      url: ${DB_URL}
#      username: ${DB_USERNAME}
#      password: ${DB_PASSWORD}
      url: "jdbc:mysql://doddev.cpezfbtz6fl6.us-east-1.rds.amazonaws.com:3306/dod_db"
      username: "usfsdlcDB"
      password: "hialan2020"
      driverClassName: "com.mysql.cj.jdbc.Driver"
jpa:
    default:
      packages-to-scan:
        - 'usf.sdlc.model'
      properties:
        hibernate:
          hbm2ddl:
            auto: update
          show_sql: true
          format_sql: true
